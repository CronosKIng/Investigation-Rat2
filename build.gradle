buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

allprojects {
    // No repositories here - all in settings.gradle
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
